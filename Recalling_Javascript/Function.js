var name = "Mert";
var age = 28;
var hasHobbies = true;

function sumUser(name, age, hasHobbies) {
  return (
    "Name is " +
    name +
    "\nAge is " +
    age +
    "\nThe user has hobbies: " +
    hasHobbies
  );
}

console.log(sumUser(name, age, hasHobbies));
